{
  "name": "ender",
  "version": "0.1.0",
  "private": true,
  "description": "Agentic runtime with iterative tool-calling using LangChain",
  "type": "commonjs",
  "main": "src/server.js",
  "workspaces": [
    "ui"
  ],
  "scripts": {
    "dev": "concurrently -k -n api,ui -c cyan,magenta \"npm:dev:api\" \"npm:dev:ui\"",
    "dev:api": "node scripts/dev-api.js",
    "dev:ui": "npm --workspace ui run dev",
    "start": "node src/server.js",
    "start:supervised": "node scripts/ender-supervisor.js",
    "start:test:api": "node src/server.js",
    "start:test:ui": "npm --workspace ui run dev -- --host 127.0.0.1 --port 4173",
    "build": "npm run build:ui",
    "build:ui": "npm --workspace ui run build",
    "test": "node --test tests/*.test.js",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:visual:update": "playwright test --update-snapshots",
    "typecheck": "tsc --project tsconfig.json",
    "check": "node --check src/server.js && node --check src/runtime/scheduleManager.js && node --check src/runtime/codeServerManager.js && node --check src/workflows/workflowManager.js && node --check src/shared/contracts.js && node --check src/selfUpdate/checkpoints.js && node --check src/selfUpdate/runner.js && node --check src/selfUpdate/manager.js && node --check src/tools/selfUpdateTools.js && node --check scripts/ender-supervisor.js",
    "verify": "npm run test && npm run typecheck && npm run check && npm run build && npm run test:e2e"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@langchain/aws": "^0.1.13",
    "@langchain/core": "^0.3.69",
    "@langchain/ollama": "^0.2.4",
    "@langchain/openai": "^0.4.5",
    "concurrently": "^9.2.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^5.1.0",
    "imapflow": "^1.0.181",
    "node-cron": "^4.2.1",
    "nodemailer": "^7.0.5",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.0",
    "@types/node": "^24.6.1",
    "@vitejs/plugin-react": "^5.1.4",
    "typescript": "^5.9.2",
    "vite": "^7.3.1"
  }
}
